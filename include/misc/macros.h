//
// Created by Blaga Lucian-Florin on 05-Mar-21.
//

#ifndef NEMULATOR_MACROS_H
#define NEMULATOR_MACROS_H

#define BETWEEN(l, v, r) ((l) <= (v) && (v) <= (r))
#define STRICT_BETWEEN(l, v, r) ((l) < (v) && (v) < (r))

#endif //NEMULATOR_MACROS_H
